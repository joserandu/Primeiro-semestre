#include <stdio.h>
#include <locale.h>
#include <math.h>

// Atividade Avaliatória - 23.04.2024

int main(void) {
	setlocale(LC_ALL, "Portuguese");
	
	int x = 5, y;
	
	switch (y){
		case 1: x * x * x;
			break;
		case 2: sqrt(x);
			break;
	}
}

